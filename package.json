{
  "name": "react-serversiderend-codesplit",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "start": "NODE_ENV=production npm run build && node modules/server.js",
    "build:client": "webpack --progress --colors",
    "build:server": "webpack --progress --colors --config webpack.server.config.js",
    "build": "npm run build:client && npm run build:server",
    "watch:client": "webpack --progress --colors --watch",
    "watch:server": "webpack --progress --colors --watch --config webpack.server.config.js",
    "watch": "npm run watch:client && npm run watch:server"
  },
  "author": "Pedro Israel",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.6.2",
    "express": "^4.14.0",
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-router": "^2.8.1"
  },
  "devDependencies": {
    "babel-core": "^6.17.0",
    "babel-loader": "^6.2.5",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-react": "^6.16.0",
    "webpack": "^1.13.2"
  }
}
